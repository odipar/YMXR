| `set.ymxr` | `bin/ymxr-multi -nCircus -nOnce circus.ymxr plays-once.ymxr` | two tunes of the other kit | 2676 | 9a6015f21de2552c | the entries, the names and the tune files of 0.2, and a name of six and one of four, so the names end on an odd byte and tune file 1 is aligned past them |
| `one.ymxb` | `bin/ymxr-bind < plays-once.ymxr` | `plays-once.ymxr` | 3172 | 40ef6bd0a9a4bfcf | a bound tune written alone: the field at 16 above 0 and the image last (1.3), and a tune of no sources, whose index is empty |
| `one.snd` | `bin/ymxr-sndh -tOnce` | `plays-once.ymxr` | 44534 | 3817ffff8c11bcd9 | one subtune that plays once, so FRMS reads its row count; no composer and no names, so COMM and !#SN are absent; the clock tag TC |
| `two.snd` | `bin/ymxr-sndh -tTwo -cJochen Hippel` | `set.ymxr` | 46424 | 48330efefae4f86b | two subtunes sharing one image, a composer, the !#SN names, FRMS of a tune that repeats beside one that plays once, and a pad before FRMS |
| `timers.snd` | `bin/ymxr-sndh -tTimers` | `four-timers.ymxr` | 45340 | ca98a55da7f6e626 | a set claiming all four timers at 60 Hz: FLAG ~abcdy and the clock tag !V (3.2), and a subtune of five sources, so the index of 1.2 is read |
| `perf.snd` | `bin/ymxr-sndh -tWatched -perf` | `plays-once.ymxr` | 45954 | 45842635fa25c778 | the core with the raster monitor in: bit 0 of the flags word (2.3), and the VBL named as the clock |
| `abs.snd` | `bin/ymxr-sndh -tAbsolute -abs` | `plays-once.ymxr` | 44182 | fafcbdc7a6f55773 | the core whose ticks read an absolute address: bit 2 of the flags word clear, where every other file of the kit has it set |
| `two.prg` | `bin/ymxr-prg -r2000` | `two.snd` | 48216 | ebdb8033446abb38 | a program of 2,000 rows: the PRG header, the stub's descriptor with bit 1 clear, and the SNDH file at the stub's end |
| `timers.prg` | `bin/ymxr-prg -vbl` | `timers.snd` | 47132 | fd72d82d54186908 | a program playing from the VBL: bit 1 of the stub's flags word (4.3), and rows 0, which plays until a key stops it |
