# BINARIES

The prebuilt binaries, and how a tool combines them with a tune file
without an assembler: the player, the program stub around it, and DTX's
reader images, each assembled once and published, and the tune file,
which holds the tune's tables and no code, combined with them into what
the player takes.

To be written in the change that makes the tune file data alone.
